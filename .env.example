# 家裡採集機用(collector/cli.ts)。複製成 .env,不進 git
RENTMAP_API=http://localhost:5173
INGEST_SECRET=local-ingest-secret
# 公車資料(npm run collect -- bus):https://tdx.transportdata.tw 註冊 → 會員中心 → API 金鑰。沒填就用匿名額度
TDX_CLIENT_ID=
TDX_CLIENT_SECRET=
